package com.dyp.core.config

interface AppConst {

    interface Router {
        object Main {
            const val A_MAIN = "/main/MainActivity"
            const val S_MAIN = "/main/MainService"
        }
    }
}